import assert from 'assert'

import { actions } from 'actions'
import { init } from 'app'


describe( 'actions', () => {
	describe( 'Deal', () => {
		it( 'deals a new table', () => {
			console.log(actions)

		})
	})
})